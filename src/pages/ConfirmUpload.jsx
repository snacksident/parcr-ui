import React from 'react';

export default function ConfirmUpload() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Confirm & Upload</h1>
      {/* TODO: Show preview of specs + images */}
      <button>Upload to Shopify</button>
    </div>
  );
}
